# Node / Next.js
node_modules/
web/.next/
web/out/
web/.vercel/

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# dane pipeline (duże, odtwarzalne przez backfill/statshub)
pipeline/data/http_cache/
pipeline/data/store/
# ...i to samo piętro niżej. Nie jest to literówka: `sts_value.py` liczył
# sobie ścieżkę sam i pomylił się o jeden katalog, więc zrzuty lądowały tutaj
# i wisiały jako nieśledzone (naprawione 2026-07-31). Wpis zostaje jako
# bezpiecznik na wypadek, gdyby ktoś powtórzył ten sam błąd.
pipeline/footstats/data/
pipeline/logs/

# obieg kursów (lokalny, może zawierać wpisane linie)
pipeline/odds/

# profile przeglądarki z testów
.browser-profile*/

# środowisko / sekrety — NIGDY do repo
.env
.env.*
!.env.example
.vercel

# systemowe
.DS_Store
Thumbs.db
web/src/data/demo/_manifest.json

# zrzuty dry-run trybu ligowego (odtwarzalne kazdym przebiegiem build_league)
web/src/data/demo/liga_dryrun/

# skrót Windows z bezwzględną ścieżką (dwuklik do odświeżenia Sofascore) —
# bezużyteczny u kogokolwiek poza autorem, więc nie ma czego wersjonować
*.lnk
